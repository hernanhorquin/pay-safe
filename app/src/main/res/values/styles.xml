<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="error_text_appearance" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/text_color_error</item>
        <item name="android:textSize">@dimen/error_text_size</item>
    </style>

    <style name="text_title_appearance">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/title_text_size</item>
        <item name="android:layout_marginTop">@dimen/title_margin_top</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="text_installment">
        <item name="android:textSize">@dimen/installment_text_size</item>
        <item name="android:layout_marginStart">@dimen/installment_margin_start</item>
        <item name="android:layout_marginEnd">@dimen/installment_margin_end</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>